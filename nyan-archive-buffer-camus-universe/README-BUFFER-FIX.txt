Buffer routing repair

- The Orphic Passage now routes directly to the Vimeo URL supplied by the author.
- Liquid Noise routes to ./liquid-noise-audio-reactive.html.
- Each active card also has a native anchor fallback, so it remains clickable even if a browser blocks the transition script.
- Click triggers a black-hole intake then navigates after 720ms.
